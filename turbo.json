{
  "$schema": "https://turborepo.dev/schema.json",
  "globalEnv": ["NEXTAUTH_SECRET", "NEXTAUTH_URL", "NEXT_PUBLIC_BACKEND_URL", "NEXT_PUBLIC_SOCKET_URL"],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
